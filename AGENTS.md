# Agent Guide

이 저장소는 10분 단위 위클리 타임블로킹 다이어리 앱의 기획/개발 문서 저장소다.

## 작업 원칙

- 먼저 `PLAN.md`를 확인해 현재 마일스톤과 실행 순서를 파악한다.
- 필요한 문서만 골라 읽는다. `docs/mvp` 전체를 한 번에 훑지 않는다.
- 구현 범위가 흔들리면 MVP 포함/제외 범위를 우선 확인한다.
- 문서를 수정할 때는 기존 한국어 톤과 마크다운 구조를 유지한다.
- 범위 밖 기능은 바로 구현하지 말고 후속 범위로 표시한다.

## 제품 기준

- 모바일 앱 우선: React Native + Expo + TypeScript.
- 데스크톱 웹은 독립 제품이 아니라 Next.js 기반 companion web이다.
- 백엔드는 Supabase Auth, Postgres, Storage를 기준으로 한다.
- 시간 기록은 10분 단위로 저장하며, 같은 날짜의 실제 기록끼리는 겹치지 않는다.
- 사진은 기록을 돕는 보조 단서다. 사진 원본 자동 업로드는 금지한다.
- MVP는 P0/P1 기록 UX, 동기화, 사진 매칭, 회고/CSV를 중심으로 한다.

## 문서 구조

- `PLAN.md`: 실제 구현용 실행 계획. 작업을 시작할 때 가장 먼저 본다.
- `기획서.md`: 제품 배경, 시장 판단, 전체 컨셉을 볼 때 참고한다.
- `docs/mvp/01-MVP-정의서.md`: MVP 목표, 타깃 사용자, 포함/제외 범위, 성공 기준.
- `docs/mvp/02-MVP-기능명세.md`: 기능별 요구사항과 수용 기준.
- `docs/mvp/03-MVP-화면설계.md`: 화면 구조, 모바일/웹 레이아웃, 화면별 완료 기준.
- `docs/mvp/04-MVP-데이터모델.md`: 엔티티, 필드, 저장 규칙, 파생 데이터, 무결성 규칙.
- `docs/mvp/05-MVP-기술설계.md`: 기술 스택, 아키텍처, 모듈, 상태 관리, 테스트 전략.
- `docs/mvp/06-MVP-개발계획.md`: 마일스톤, 우선순위 백로그, QA, 출시 기준.

### 문서 선택 기준

- 기능 동작을 확인할 때: `docs/mvp/02-MVP-기능명세.md`
- 화면/UX를 확인할 때: `docs/mvp/03-MVP-화면설계.md`
- 데이터 구조나 계산 규칙을 확인할 때: `docs/mvp/04-MVP-데이터모델.md`
- 기술 선택이나 모듈 경계를 확인할 때: `docs/mvp/05-MVP-기술설계.md`
- 일정, 우선순위, 체크리스트를 확인할 때: `PLAN.md`와 `docs/mvp/06-MVP-개발계획.md`

## 토큰 절약 규칙

- 답변 전에 필요한 파일명과 섹션을 먼저 좁힌다.
- 문서 전문을 요약하지 말고, 작업과 직접 관련된 항목만 인용/반영한다.
- 이미 `PLAN.md`에 있는 체크리스트는 중복 작성하지 않는다.
- 큰 변경 전에는 관련 문서의 제목/섹션만 먼저 검색한 뒤 필요한 줄만 읽는다.

## Sub-agent 규칙

- 단일 파일 수정이나 작은 문서 작업에는 사용하지 않는다.
- 큰 구현에서 병렬 조사가 명확히 유리할 때만 사용한다.
- 사용할 때는 역할과 파일 범위를 좁게 나누고 중복 조사를 피한다.

## Git 규칙

- 사용자가 명시적으로 요청하기 전에는 commit, push, PR 생성을 하지 않는다.
- 커밋 전에는 `git status --short`로 변경 범위를 확인한다.
- 작업과 무관한 파일은 stage하지 않는다.
- 사용자가 만든 변경을 되돌리지 않는다.
- 커밋 메시지는 한국어로 간결하게 작성한다.
- 커밋 메시지 형식은 `type: 내용`을 사용한다.
  - 예: `docs: MVP 개발계획 정리`
  - 예: `feat: 시간 기록 도메인 타입 추가`
  - 예: `fix: 기록 겹침 계산 수정`
